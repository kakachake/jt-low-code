import { FieldNode } from '../typs'

const baseFields: FieldNode[] = [
  {
    type: 'div',
    module: 'base',
    props: {
      className: '',
      children: ['div组件']
    }
  }
]

export default baseFields
